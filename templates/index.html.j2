{% extends 'base.html.j2' %}
{% block content %}
<style>
  /* Reuse container */
  .container { max-width: 1200px; margin: 0 auto; padding: 20px; }

  /* Hero Section */
  .hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 60px 20px;
  }
  .hero h1 {
    font-size: 2.5rem;
    margin-bottom: 16px;
    text-align: center;
  }
  .hero p {
    font-size: 1.1rem;
    max-width: 600px;
    text-align: center;
    margin-bottom: 32px;
  }
  .hero button {
    padding: 12px 32px;
    border-radius: 50px;
    background: #e0e0e0;
    box-shadow: 4px 4px 8px #bebebe, -4px -4px 8px #ffffff;
    border: none;
    cursor: pointer;
    transition: box-shadow 0.3s;
    font-size: 1rem;
  }
  .hero button:hover {
    box-shadow: inset 4px 4px 8px #bebebe, inset -4px -4px 8px #ffffff;
  }

  /* Section Headings */
  .container > h2 {
    text-align: center;
    font-size: 1.8rem;
  }

  /* About Project */
  .neocard.container {
    padding: 20px;
  }
  .neocard.container h2 {
    margin-bottom: 16px;
  }

  /* Features Section */
  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
  }
  .feature-item {
    text-align: center;
  }
  /* Icon with Neomorphic and custom float animation */
  @keyframes floatIcon {
    0% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-10px) scale(1.1); }
    100% { transform: translateY(0) scale(1); }
  }
  .feature-item .icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 12px;
    background: var(--neo-bg);
    border-radius: 50%;
    box-shadow: 6px 6px 12px var(--neo-shadow-dark), -6px -6px 12px var(--neo-shadow-light); /* Ganti dari #bebebe, #fff */
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    animation: floatIcon 3s ease-in-out infinite;
    transition: background 0.3s, box-shadow 0.3s;
  }

  /* Form Section */
  .form-section {
    max-width: 600px;
    margin: 0 auto 40px;
    padding: 30px;
    background: #e0e0e0;
    border-radius: 16px;
    box-shadow: 8px 8px 16px #bebebe, -8px -8px 16px #ffffff;
  }
  .form-section h2 {
    text-align: center;
    margin-bottom: 24px;
  }
  .form-group { margin-bottom: 20px; }
  .form-group label {
    display: block;
    margin-bottom: 8px;
    font-weight: bold;
  }
  .form-group input[type="number"] {
    width: 100%;
    padding: 12px;
    border: none;
    border-radius: 12px;
    background: #e0e0e0;
    box-shadow: inset 4px 4px 8px #bebebe, inset -4px -4px 8px #ffffff;
    font-size: 1rem;
  }
  .form-group input[type="number"]:focus {
    outline: none;
    box-shadow: inset 2px 2px 4px #bebebe, inset -2px -2px 4px #ffffff;
  }
  .form-section button {
    display: block;
    margin: 20px auto 0;
    padding: 14px 32px;
    border-radius: 50px;
    background: #e0e0e0;
    box-shadow: 6px 6px 12px #bebebe, -6px -6px 12px #ffffff;
    border: none;
    cursor: pointer;
    transition: box-shadow 0.3s;
    font-size: 1rem;
    font-weight: bold;
  }
  .form-section button:hover {
    box-shadow: inset 6px 6px 12px #bebebe, inset -6px -6px 12px #ffffff;
  }

  /* Footer */
  footer {
    text-align: center;
    font-size: 0.9rem;
    color: #777;
  }
</style>

<!-- Hero -->
<div class="container hero animate__animated animate__fadeInDown">
  <h1 class="animate__animated animate__fadeInUp">Sistem Pendukung Keputusan Pemilihan Ras Kucing</h1>
  <p class="animate__animated animate__fadeInUp">Atur bobot kriteria sesuai preferensi Anda, dan sistem akan merekomendasikan ras kucing terbaik menggunakan metode Simple Additive Weighting (SAW).</p>
  <button class="animate__animated animate__pulse animate__infinite" onclick="document.getElementById('prefs').scrollIntoView({ behavior: 'smooth' });">Mulai Sekarang</button>
</div>

<!-- About Project -->
<section class="neocard container animate__animated animate__fadeInUp">
  <h2>Tentang Proyek</h2>
  <p>Sistem ini membantu pecinta kucing memilih ras yang paling sesuai berdasarkan kriteria seperti tingkat perawatan, keaktifan, tingkat alergi, dan biaya. Metode SAW digunakan untuk menghitung skor tiap ras berdasarkan bobot yang Anda tentukan.</p>
</section>

<!-- Features -->
<section class="container animate__animated animate__fadeInUp">
  <h2>Fitur Utama</h2>
  <div class="features">
    <div class="feature-item animate__animated animate__fadeInLeft">
      <div class="icon">🐱</div>
      <h3>Data Lengkap Ras</h3>
      <p>Puluhan ras kucing, lokal hingga internasional, siap dieksplorasi.</p>
    </div>
    <div class="feature-item animate__animated animate__fadeInRight">
      <div class="icon">⚙️</div>
      <h3>Metode SAW</h3>
      <p>Objektif dan transparan, memastikan rekomendasi yang akurat.</p>
    </div>
    <div class="feature-item animate__animated animate__fadeInLeft">
      <div class="icon">🎯</div>
      <h3>Personalisasi</h3>
      <p>Atur bobot pada setiap kriteria sesuai prioritas Anda.</p>
    </div>
    <div class="feature-item animate__animated animate__fadeInRight">
      <div class="icon">📱</div>
      <h3>Responsif</h3>
      <p>Tampilan optimal di desktop maupun perangkat seluler.</p>
    </div>
  </div>
</section>

<!-- Form Section -->
<section id="prefs" class="form-section animate__animated animate__fadeInUp">
  <h2>Masukkan Bobot Kriteria</h2>
  <form action="{{ url_for('hasil') }}" method="post">
    {% for k in kriteria %}
      <div class="form-group animate__animated animate__fadeInUp animate__delay-1s">
        <label for="bobot_{{ k.id }}">{{ k.nama_kriteria }} ({{ k.sifat }})</label>
        <input type="number" id="bobot_{{ k.id }}" name="bobot_{{ k.id }}" step="0.01" min="0" max="1" required placeholder="Masukkan bobot 0 - 1">
      </div>
    {% endfor %}
    <button type="submit" class="animate__animated animate__fadeInUp animate__delay-2s">Hitung Rekomendasi</button>
  </form>
</section>
{% endblock %}
